#include <bits/stdc++.h>
using namespace std;


void sumOfNumbers(string s) {
    int n = s.size(), ans = 0;

    for(int i=0; i<n; i++) {
        int ascii = (int) s[i];

        if(ascii >= 48 && ascii <= 57) {
            // ans += 
        }
    }
}


int main()
{

    string str = "1223";

    cout << atoi(str[0]) << endl;

    return 0;
}